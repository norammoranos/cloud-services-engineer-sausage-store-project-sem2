---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.global.vault.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  vault_token: {{ required "global.vault.vaultToken is required for backend Vault integration" .Values.global.vault.vaultToken | quote }}
